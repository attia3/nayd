% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_06_process_county_year.R
\name{process_county_year}
\alias{process_county_year}
\title{Process a single county-year (NAYD pipeline)}
\usage{
process_county_year(
  w_norm_orig,
  county_geom,
  max_field_ha_thresh,
  tile_target_ha,
  min_field_ha,
  max_field_ha_final,
  cluster_dist_m,
  nass_area_ha,
  nass_yield_kg_ha,
  county_name,
  crop_name,
  year_val,
  beta,
  min_frac = NULL,
  max_frac = NULL,
  y_min_crop = NULL,
  y_max_crop = NULL,
  min_abs = NULL
)
}
\arguments{
\item{w_norm_orig}{SpatRaster; county/year weight raster (any CRS, will be
reprojected to EPSG:5070 internally).}

\item{county_geom}{sf; county polygon geometry (any CRS).}

\item{max_field_ha_thresh}{numeric; max area (ha) for a “natural” field
before tiling into pseudo-units.}

\item{tile_target_ha}{numeric; target area (ha) for fishnet tiles in large
fields (e.g. ~200 ha).}

\item{min_field_ha}{numeric; minimum allowed harvest-unit area (ha).}

\item{max_field_ha_final}{numeric; maximum allowed harvest-unit area (ha)
after tiling.}

\item{cluster_dist_m}{numeric; cut height (meters) for hierarchical
clustering of unit centroids (e.g. 10000 for ~10 km).}

\item{nass_area_ha}{numeric; NASS harvested area (ha) for this county-year.}

\item{nass_yield_kg_ha}{numeric; NASS county mean yield (kg/ha).}

\item{county_name}{character; county name.}

\item{crop_name}{character; crop name (used by disagg caps).}

\item{year_val}{integer; year.}

\item{beta}{numeric; exponent applied to unit-level weights.}

\item{min_frac, max_frac}{optional numeric; relative min/max yield fractions
vs NASS mean. If \code{NULL}, the disagg function will rely on crop caps.}

\item{y_min_crop, y_max_crop, min_abs}{optional numeric; crop-specific caps.
If \code{NULL}, defaults are taken from \code{.get_crop_caps(crop_name)}.}
}
\value{
A list with elements:
\describe{
\item{prep}{list with reprojected weight raster and capped weights}
\item{seg}{segmentation objects (units before harvest selection)}
\item{harv}{selected harvest units}
\item{dis}{units with disaggregated yield + QA check}
\item{clusters}{final merged clusters (sf + table)}
\item{info}{county/year metadata}
}
}
\description{
Process a single county-year (NAYD pipeline)
}
